#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
  * main - program antry point
  *
  * Return: Always Return 0
  */

int main(void)
{
	char c;

	c = 48;
	while (c < 58)
	putchar(c++);
	c = 97;
	while (c < 103)
	putchar(c++);
	putchar('\n');
	return (0);
}
