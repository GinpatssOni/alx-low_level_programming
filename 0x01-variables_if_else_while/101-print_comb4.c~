#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
  * main - program antry point
  *
  * Return: Always Return 0
  */

int main(void)
{
	int c;
	int c2;
	int rmn;

	c = 48;
	c2 = 49;
	rmn = 49;
	while (c < 57)
	{
		while (c2 < 58)
		{
		putchar(c);
		putchar(c2++);
		if (c == 56)
		continue;
		putchar(',');
		putchar(' ');
		}
	c++;
	c2 = ++rmn;
	}
	putchar('\n');
	return (0);
}
