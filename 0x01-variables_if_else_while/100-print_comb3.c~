#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
  * main - program antry point
  *
  * Return: Always Return 0
  */

int main(void)
{
	int c;

	c = 48;
	while (c < 58)
	{
	putchar(c++);
		if (c == 58)
		continue;
	putchar(',');
	putchar(' ');
	}
	putchar('\n');
	return (0);
}
